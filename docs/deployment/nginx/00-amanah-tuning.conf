# /etc/nginx/conf.d/00-amanah-tuning.conf  -- root:root, 0644, not secret.
#
# The operator key is 64 hex chars, which overflows nginx's default
# map_hash_bucket_size of 64 and makes the whole config fail to build.
# Named 00- so it is read before the map that needs it.
map_hash_bucket_size 128;
